package una.jwtdemo01;

/**
 * Archivo heredado de un proyecto de demo anidado dentro del repositorio.
 *
 * IMPORTANTE:
 * Este directorio está dentro de <code>src/main/java</code> del backend principal,
 * por lo que IntelliJ puede intentar compilarlo como parte del módulo.
 * Para evitar errores por dependencias de test (JUnit / SpringBootTest),
 * se deja como clase vacía.
 */
public class JwtDemo01ApplicationTests {
}
